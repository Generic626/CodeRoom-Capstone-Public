const UserAvatarList = ({ avatarList }) => {
  // if avatarList is greater than 3
  // show avtar user.a + user.b + (avatarList.length-2)
  // on hover show the user's email
  return <div>UserAvatarList</div>;
};

export default UserAvatarList;
